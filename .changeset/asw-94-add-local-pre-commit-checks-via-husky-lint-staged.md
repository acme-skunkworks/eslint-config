---
"@acme-skunkworks/eslint-config": patch
---

Add local git hooks (husky + lint-staged) for pre-commit auto-fix, `Co-Authored-By: Claude` trailer strip, and direct-push-to-`main` block. Tooling only — no runtime change for consumers.
