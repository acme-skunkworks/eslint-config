---
"@acme-skunkworks/eslint-config": major
---

Extract the ESLint config from `RobEasthope/protomolecule` into a standalone repo and rename the package to `@acme-skunkworks/eslint-config`. The exported flat-config array is unchanged from `@robeasthope/eslint-config@6.2.1`; consumers only need to update their dependency name and import path. See `MIGRATION_FROM_PROTOMOLECULE.md` for full details.
