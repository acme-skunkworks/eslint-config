# Release packing ephemerals (release.yml packs into .pack/)
*.tgz
.pack/

# actionlint binary fetched by infrastructure/scripts/ensure-actionlint.sh
/actionlint

node_modules/
dist/
build/
coverage/

.turbo/
.eslintcache*
*.tsbuildinfo

.DS_Store
*.log
.npm/
.pnpm-store/

.env
.env.local
.env.*.local

# act reads .secrets by default for secret injection — ignore to prevent
# accidental commits if anyone later experiments with `act --secret-file`.
.secrets

.idea/
.vscode/
*.swp
*.swo
