# npm access token — manual publish backup only.
#
# Primary path: CI publishes via OIDC Trusted Publishing (no token needed).
# This token is only for human-driven manual publishes when CI is unavailable.
#
# Generate at: https://www.npmjs.com/settings/<your-username>/tokens
#   Type: Granular Access Token (preferred) or Automation
#   Scope: read+write for @acme-skunkworks/eslint-config
#   Expiry: 30–90 days recommended
#
# To use:
#   1. Copy this file to .env and fill in the token, OR
#   2. Run `npm login` once (browser flow) and skip the token entirely —
#      `npm publish` will prompt for an OTP from your authenticator.
#
# Never commit .env. It's already in .gitignore.
NPM_TOKEN=
